spring:
  datasource:
    url: jdbc:mysql://localhost:3307/SRM_prod
    username: SRM_prod_user
    password: admin
    platform: mysql
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    database: mysql
    show-sql: false